package com.mohamedrejeb.richeditor.sample.common.notion

import androidx.compose.ui.graphics.Color

/**
 * Palette tuned to Notion's clean, warm whitespace aesthetic. Numbers come from the
 * public Notion web client so the sample reads as "Notion" without being an exact rip.
 */
internal object NotionColors {
    val Background = Color(0xFFFFFFFF)
    val PageSurface = Color(0xFFFFFFFF)
    val SidebarSurface = Color(0xFFF7F6F3)
    val Divider = Color(0xFFE9E9E7)
    val Border = Color(0xFFEDEDEC)

    val TextPrimary = Color(0xFF37352F)
    val TextSecondary = Color(0xFF787774)
    val TextMuted = Color(0xFF9B9A97)
    val PlaceholderText = Color(0xFFBAB9B5)

    val Hover = Color(0xFFF1F1EF)
    val HoverStrong = Color(0xFFEAEAE8)
    val MenuSurface = Color(0xFFFFFFFF)
    val MenuHighlight = Color(0xFFEFEFEE)
    val MenuShadow = Color(0x14000000)

    val Link = Color(0xFF0B6E99)
    val MentionText = Color(0xFF2E7CD6)
    val MentionBackground = Color(0xFFE7F1FC)

    val CodeText = Color(0xFFE03E3E)
    val CodeBackground = Color(0xFFF6F4F3)
    val CodeStroke = Color(0xFFE9E9E7)

    val QuoteBar = Color(0xFF37352F)
    val SlashAccent = Color(0xFF9B9A97)

    val IconTint = Color(0xFF37352F)
    val IconMuted = Color(0xFF787774)
}
